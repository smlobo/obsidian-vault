* `std::expected`
	* c++23
	* return value  or `std::string` of error
* `std::fill`
* `std::for_each`
* `std::function<>`
	* pass lambda as parameter
* `std::optional`
	* return value or `std::nullopt`
* `std::sort`
* `std::span`
	* A reference to a vector/array/built-in (c-style) array
	* refers to the old container on reallocation
	* good for function argument